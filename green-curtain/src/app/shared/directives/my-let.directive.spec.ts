import { MyLetDirective } from './my-let.directive';

describe('MyLetDirective', () => {
  it('should create an instance', () => {
    const directive = new MyLetDirective();
    expect(directive).toBeTruthy();
  });
});
