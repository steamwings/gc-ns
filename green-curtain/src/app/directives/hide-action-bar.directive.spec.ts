import { HideActionBarDirective } from './hide-action-bar.directive';

describe('HideActionBarDirective', () => {
  it('should create an instance', () => {
    const directive = new HideActionBarDirective();
    expect(directive).toBeTruthy();
  });
});
